import { DefaultButton } from "ui";

export default function Web() {
  return (
    <div>
      <h1>Web</h1>
      <DefaultButton size="md" isLoading={false}>
        버튼
      </DefaultButton>
    </div>
  );
}
